# API测试用例设计

> **文档类型**：概要设计 — API测试用例
> **作者**：Agent-Test-Docs（测试/文档工程师）
> **日期**：2026-07-01
> **任务ID**：D4-T05
> **依赖**：D4-T01(API详细规范)
> **状态**：待审查

---

## 1. 测试策略

| 维度 | 策略 |
|------|------|
| 框架 | pytest + Flask test client |
| 覆盖 | 每个端点 ≥1正常 + ≥1异常 |
| 数据 | 测试数据库（SQLite内存），每次测试前seed，测试后teardown |
| 认证 | 测试fixture提供admin_token和analyst_token |

---

## 2. Auth模块测试用例（6条）

| ID | 端点 | 场景 | 预期 |
|----|------|------|------|
| TC-A-01 | POST /auth/login | 正确用户名密码 | 200, 返回token+user |
| TC-A-02 | POST /auth/login | 错误密码 | 401 |
| TC-A-03 | POST /auth/login | 空body | 400 |
| TC-A-04 | POST /auth/register | admin创建analyst | 201 |
| TC-A-05 | POST /auth/register | 重复用户名 | 409 |
| TC-A-06 | POST /auth/refresh | 有效Token | 200, 新Token |

---

## 3. Sections模块（6条）

| ID | 端点 | 场景 | 预期 |
|----|------|------|------|
| TC-S-01 | GET /sections | 无参数 | 200, 分页返回 |
| TC-S-02 | GET /sections | ?page=999 | 200, items=[] |
| TC-S-03 | GET /sections/{id} | 存在ID | 200 |
| TC-S-04 | GET /sections/{id} | 不存在ID | 404 |
| TC-S-05 | POST /sections | admin+有效数据 | 201 |
| TC-S-06 | POST /sections | analyst角色 | 403 |

---

## 4. Traffic模块（4条）

| ID | 端点 | 场景 | 预期 |
|----|------|------|------|
| TC-T-01 | GET /traffic/current | 无参数 | 200, 返回全部路段 |
| TC-T-02 | GET /traffic/current | ?section_id=1 | 200, 单条 |
| TC-T-03 | GET /traffic/history | 有效时间范围 | 200, 分页 |
| TC-T-04 | GET /traffic/history | 无数据时间范围 | 200, items=[] |

---

## 5. Prediction模块（6条）

| ID | 端点 | 场景 | 预期 |
|----|------|------|------|
| TC-P-01 | GET /predict/forecast | section_id=1&model=RF | 200 |
| TC-P-02 | GET /predict/forecast | model=KNN | 200 |
| TC-P-03 | GET /predict/forecast | model=INVALID | 400 |
| TC-P-04 | GET /predict/forecast | 不存在的section_id | 404 |
| TC-P-05 | GET /predict/accuracy | section_id=1 | 200, 含双模型对比 |
| TC-P-06 | GET /predict/forecast | 无认证Header | 401 |

---

## 6. Warning模块（5条）

| ID | 端点 | 场景 | 预期 |
|----|------|------|------|
| TC-W-01 | GET /warning/list | 无参数 | 200, 分页 |
| TC-W-02 | GET /warning/list | ?level=CRITICAL&is_resolved=false | 200, 仅未解除严重预警 |
| TC-W-03 | PUT /warning/{id}/resolve | admin | 200 |
| TC-W-04 | PUT /warning/{id}/resolve | analyst | 403 |
| TC-W-05 | GET /warning/rules | — | 200, 返回阈值配置 |

---

## 7. Route模块（4条）

| ID | 端点 | 场景 | 预期 |
|----|------|------|------|
| TC-R-01 | POST /route/plan | 有效起终点 | 200, 含路径序列 |
| TC-R-02 | POST /route/plan | 不可达路径 | 422 |
| TC-R-03 | POST /route/plan | 相同起终点 | 422 |
| TC-R-04 | GET /route/{id} | 存在ID | 200 |

---

## 8. 统计与边界测试（5条）

| ID | 场景 | 预期 |
|----|------|------|
| TC-E-01 | 请求body缺少必填字段 | 400 |
| TC-E-02 | page_size=1000（超过max） | 400或自动截断为100 |
| TC-E-03 | 并发同一资源创建 | 一个201一个409 |
| TC-E-04 | XSS注入 `username="<script>"` | 400或正常存储（转义） |
| TC-E-05 | SQL注入 `?name=' OR '1'='1` | 200, items=[]（参数化查询防护） |

---

## 9. 测试fixture设计

```python
# backend/tests/conftest.py
import pytest
from app import create_app
from app.models import db

@pytest.fixture
def app():
    app = create_app('testing')  # SQLite内存数据库
    with app.app_context():
        db.create_all()
        seed_test_data()
        yield app
        db.drop_all()

@pytest.fixture
def client(app):
    return app.test_client()

@pytest.fixture
def admin_token(client):
    res = client.post('/api/v1/auth/login', json={
        'username': 'admin', 'password': 'test123'
    })
    return res.json['data']['token']

@pytest.fixture
def analyst_token(client):
    res = client.post('/api/v1/auth/login', json={
        'username': 'analyst_test', 'password': 'test123'
    })
    return res.json['data']['token']
```

---

## 10. 汇总

| 模块 | 用例数 | 正常 | 异常 | 边界 |
|------|--------|------|------|------|
| Auth | 6 | 3 | 3 | 0 |
| Sections | 6 | 3 | 2 | 1 |
| Traffic | 4 | 3 | 0 | 1 |
| Prediction | 6 | 3 | 3 | 0 |
| Warning | 5 | 3 | 1 | 1 |
| Route | 4 | 2 | 2 | 0 |
| 通用 | 5 | 0 | 4 | 1 |
| **合计** | **36** | **17** | **15** | **4** |

---

## 11. 交付清单

| 交付物 | 章节 |
|--------|------|
| 7模块36条测试用例 | 第2-8节 |
| 正常/异常/边界三级覆盖 | 第10节汇总表 |
| pytest fixture设计 | 第9节 |
